@model IEnumerable<Hotel_Management_Project.Models.Image>

@{
    ViewBag.Title = "Images";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ImageLink)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HOTEL.nomHotel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ImageLink)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HOTEL.nomHotel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDimage }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDimage }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDimage })
        </td>
    </tr>
}

</table>
